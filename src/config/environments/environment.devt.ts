import { EnvironmentEnum } from "@config/enums/environment.enum";
import { IEnvironment } from "@config/interfaces/environment.interfaces";

export const environment: IEnvironment = {
    name: EnvironmentEnum.DEVELOPMENT
};
