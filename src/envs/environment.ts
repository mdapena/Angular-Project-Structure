import { EnvironmentEnum } from "./enums/environment.enum";
import { IEnvironment } from "./interfaces/environment.interfaces";

export const environment: IEnvironment = {
    name: EnvironmentEnum.PRODUCTION
};
